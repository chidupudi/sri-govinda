import { Zoom, Fade } from '@mui/material';
import { AccountCircle, ShoppingCart, Assessment } from '@mui/icons-material';

// Usage example:
<Zoom in={true}>
  <Button startIcon={<AccountCircle />}>Profile</Button>
</Zoom>